// TODO delete this file once the /templates directory contains files
// Heroku barfs on the fact that application.js requires a /templates dir but
// the /templates dir does not contain any files.  Also the Github repo
// does not add folders with no files in them.